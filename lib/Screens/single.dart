import 'package:flutter/material.dart';

class Single extends StatelessWidget {
  const Single({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: const Text('confetti tester')),
        body: const Center());
  }
}
